# GitLab Configuration
GITLAB_TOKEN=your_gitlab_token_here
GITLAB_URL=https://source.golabs.io

# Google Drive Configuration (Optional)
# Required only if using --with-drive flag
GOOGLE_DRIVE_TOKEN=your_google_drive_access_token_here
MCP_DRIVE_URL=drive.taraai.tech

# LLM Configuration
# The system uses two LLMs in collaboration:
#   - gpt-oss: For GitLab data fetching and Google Drive search (tool calling)
#   - sahabat-4bit: For documentation writing
# Both are automatically configured, no setup needed.
